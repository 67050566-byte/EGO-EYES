## Build Log

สิ่งที่ทำเสร็จแล้วใน Sprint 3

* เชื่อมต่อเซ็นเซอร์วัดระยะเข้ากับบอร์ด ESP32 สำเร็จ
* เขียนโปรแกรมสำหรับอ่านค่าระยะจากเซ็นเซอร์ได้
* แสดงค่าระยะผ่าน Serial Monitor ได้
* ระบบสามารถตรวจจับระยะใกล้หน้าจอได้จริง
* ทำระบบแจ้งเตือนเมื่อผู้ใช้อยู่ใกล้หน้าจอเกินกำหนด
* ทดสอบการทำงานของ Input → Process → Output ร่วมกัน
* จัดทำ Demo Evidence และอัปเดต GitHub ของทีม
